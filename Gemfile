source 'https://rubygems.org'

# Ruby client for DataDog's API http://www.datadoghq.com
gem 'dogapi'

# Offical Rubyzip repository
gem 'rubyzip'

# An AES encrypt/decrypt gem built ontop of OpenSSL.
gem 'aes', '~> 0.5.0'

# Your Rails variables in your JS
gem 'gon'

# The official AWS SDK for Ruby.
gem 'aws-sdk', '~> 2'

# The express way to send mail from Ruby.
gem 'pony'
# Preview mail in the browser instead of sending.
gem 'letter_opener', :group => :development

# Resque is a Redis-backed Ruby library for creating background jobs, placing them on multiple queues, and processing them later
gem 'resque'
# A light-weight job scheduling system built on top of resque
gem 'resque-scheduler'
# resque-scheduler doesn't work with rufus-scheduler 3.x
gem 'rufus-scheduler', '2.0.24'

# A Ruby client library for Redis
gem 'redis'

# Ruby process monitor
gem 'god'

# The official Airbrake library for Ruby on Rails
gem 'airbrake'

# Generates attr_accessors that encrypt and decrypt attributes
gem 'attr_encrypted'

# An IRB alternative and runtime developer console
group :development do
  gem 'pry'
  gem 'pry-doc'
  gem 'pry-rails'
end

# Remote multi-server automation tool
gem 'capistrano', '~> 3.1.0'
gem 'capistrano-rvm'
gem 'capistrano-rails', '~> 1.1'
gem 'capistrano-bundler', '~> 1.1.2'

# https://github.com/capistrano/rails/issues/111
gem 'sprockets', '~> 2.8'

# Barebones two-factor authentication with Devise
gem 'devise-two-factor', '~> 1.1'
# Flexible authentication solution for Rails with Warden
gem 'devise'

# Twitter Bootstrap for Rails 3.x - 4 Asset Pipeline
gem 'twitter-bootstrap-rails'

# Ruby Implementation of the BSON Specification (2.0.0+)
gem 'bson', '~> 3.0'
# Ruby driver for MongoDB
gem 'mongo', '~> 2.0'

#ODM (Object-Document-Mapper) framework for MongoDB in Ruby
gem 'mongoid', '~> 5.1.0'

# Mysql2 - A modern, simple and very fast MySQL library for Ruby - binding to libmysql
# Latest version(0.4) of mysql2 gem doesn't work well with rails 4.2.4
gem 'mysql2', '~> 0.3.20'

# Simple, Heroku-friendly Rails app configuration using ENV and a single YAML file
gem 'figaro'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.4'
# Use sqlite3 as the database for Active Record
gem 'sqlite3'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# See https://github.com/rails/execjs#readme for more supported runtimes
gem 'therubyracer', platforms: :ruby

# HTTP accessing library
gem 'httpclient'
# flexible HTTP client library
gem 'faraday'

# jQuery UI for the Rails asset pipeline
gem 'jquery-ui-rails'
# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Provides a health-check endpoint to your Ruby on Rails apps.
gem 'okcomputer'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use Unicorn as the app server
# gem 'unicorn'

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'
end

group :development do
  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
end



ruby '2.2.2'
